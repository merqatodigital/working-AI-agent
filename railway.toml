[build]
builder = "dockerfile"

[deploy]
startCommand = "uv run --package agent-api uvicorn agent_api.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 10
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3